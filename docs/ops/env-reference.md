# Environment reference (full build)

Every variable, per phase. Provision in Vercel for **preview + production**
(plan §10 §2). ⚙ = generate yourself and store in your secret manager:
`openssl rand -base64 32`.

| Variable | Phase | Notes |
|---|---|---|
| `OPENAI_API_KEY` | existing | engine calls |
| `OPENAI_BASE_URL` | WS-2 | Optional OpenAI-compatible endpoint. **Production allows only the OpenRouter host (`openrouter.ai`)** — the decided provider architecture; every other compatible host stays evaluation-only (preview/test). HTTPS and no-credentials-in-URL are enforced everywhere (`lib/model-transport.ts`). All three transports follow it in lockstep: the text engine and both vision drafters (meal photo, Pantry). Set it together with provider-prefixed `PAL_MODEL` and `PAL_VISION_MODEL` (e.g. `openai/gpt-5.4-mini`) — a mismatched pair fails loudly before any paid call. **Unset it to fall back to direct OpenAI** (the rollback path — no redeploy needed, but model ids must drop their prefix in the same change). |
| `PAL_MODEL` | existing | Text-engine model id (default `gpt-5.4-mini`). Provider-prefixed (`openai/…`) **iff** `OPENAI_BASE_URL` targets OpenRouter; unprefixed on direct OpenAI. |
| `PAL_VISION_MODEL` | WS-2 | Vision model id for meal-photo drafts and Pantry extraction (default `gpt-5.4-mini`). Same prefix rule as `PAL_MODEL` — keep it in lockstep with `OPENAI_BASE_URL`. |
| `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` | existing | rate limit; prod instance |
| `SENTRY_DSN` | existing/P7 | server-only capture |
| `EDGE_CONFIG` | existing | launch-controls kill switch |
| `DATABASE_URL` | 4A | **Restricted runtime-role** Postgres URL — since the 2026-08-10 outage migration this is **Neon** (project `dry-shadow-56131409`, Vercel integration resource `revora-db`; runtime role `prediabetespal_app` — the Stage D rename was completed 2026-08-11 and `revora_app` dropped, `docs/runbooks/database-governance.md` §"One-time role split"), not the Railway instance `docs/adr/hosting-hybrid.md` describes — DML on app tables, no database/schema `CREATE`; the app connects via `pg`/`drizzle-orm/node-postgres` with a bounded per-instance pool (default `max: 3`). ⚠️ 2026-08-16: the **Preview**-scope value is a placeholder, not a URL — see the launch checklist §1 |
| `DATABASE_POOL_MAX` | I-15 | Optional per-instance pool cap, integer `1..10`, default `3`. Do not raise it to hide provider connection pressure; follow `docs/runbooks/database-governance.md` |
| `DATABASE_MIGRATION_URL` | I-15 / operator CLI only | Owner/migrator URL used by Drizzle (Neon `neondb_owner` since 2026-08-10; was Railway). Never bind it to Vercel runtime. `PAL_DB_ENV=production` requires this URL, requires `DATABASE_URL`, and rejects identical usernames |
| `PAL_DB_ENV` | I-15 / operator CLI only | Set to exact `production` only for the protected production migration command. Do not set in Vercel; it exists to make `npm run db:migrate:production` fail closed on missing/undivided roles |
| ⚙ `AUTH_SECRET` | 4A | Auth.js session/token signing |
| ⚙ `HEALTH_DATA_KEY` | 4A | **exactly 32 bytes base64** — AES-256-GCM key for A1C + food text. Losing it orphans all ciphertext; store it like a root credential. **Never replace it in isolation** — rotation has a procedure: `docs/runbooks/health-key-rotation.md` (PR-2) |
| `HEALTH_DATA_KEY_VERSION` | PR-2 | Integer stamped into new ciphertext payloads (`v<n>:` prefix). Default `1`. Bump only as part of the rotation runbook |
| `HEALTH_DATA_KEYS_OLD` | PR-2 | Retired **decrypt-only** keys, `version:base64key` comma-separated (e.g. `1:AAAA…`). **INVARIANT: never drop a key while any row encrypted under it exists** — decryption fails quietly and the data is silently lost. See `docs/runbooks/health-key-rotation.md` |
| `RESEND_API_KEY` | 4A | magic-link email |
| `RESEND_WEBHOOK_SECRET` | email delivery | Signing secret for `POST /api/webhooks/resend`. Required in production; the route verifies the raw body plus `svix-id`, `svix-timestamp`, and `svix-signature` before updating PII-minimized delivery/suppression state. Subscribe the provider endpoint to `email.sent`, `email.delivered`, `email.delivery_delayed`, `email.bounced`, `email.complained`, `email.suppressed`, and `email.failed`. |
| `AUTH_EMAIL_FROM` | 4A | e.g. `Revora <signin@yourdomain>`; domain must be Resend-verified |
| `AUTH_EMAIL_STUB_DIR` | dev/test only | writes magic links to disk instead of sending — never set in production |
| `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON` | 4D | full JSON of the Play API service account |
| `PLAY_PACKAGE_NAME` | 4D | e.g. `com.prediabetespal.twa` |
| ⚙ `RTDN_SHARED_TOKEN` | 4D | shared token on the Pub/Sub push endpoint URL |
| `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` | 4D | web-fallback billing |
| `STRIPE_PRICE_MONTHLY` / `STRIPE_PRICE_ANNUAL` | 4D | Stripe price IDs for the two legacy SKUs |
| `PAYWALL_MODE` | launch-readiness | **The business-model flag.** `trial` = card-gated 7-day trial + hard wall (the locked 2026-07-05 model); `legacy` = the explicit rollback lever to the standing free tier (5 checks/day). **Defaults to `trial`** — `lib/server/pricing.ts` treats anything other than exact `legacy` as `trial` (WS-7 doc correction; the previous "defaults to legacy" note was wrong). Set `PAYWALL_MODE=legacy` deliberately to roll the business model back |
| `LEGAL_TERMS_FINAL` | W-04 kill switch | **Paid-checkout kill switch** (`checkoutGate()`, `app/api/billing/handlers.ts`): checkout is **OPEN by default** — only the exact value `0` closes every paid Stripe entry point (503) without a redeploy. Semantics inverted from the original default-closed `=1` gate by owner WTP decision 2026-07-17 (commit `8c30265`), after `/terms` passed the placeholder-free smoke test; counsel sign-off remains deferred (`docs/legal/owner-risk-launch-decision-5f6abcb.md` still reads `COUNSEL GATE: NOT CLEARED`). `/api/health` reports the same predicate as `checkoutGate` |
| `TRIAL_PRICE_VARIANT` | launch-readiness | `999` / `1299` (default) / `1999` — selects the monthly price cohort for the trial wall |
| `STRIPE_PRICE_MONTHLY_999` / `STRIPE_PRICE_MONTHLY_1299` / `STRIPE_PRICE_MONTHLY_1999` | launch-readiness | live Stripe price IDs for the three trial-mode monthly variants (`lib/server/pricing.ts`) |
| `STRIPE_PRICE_PANTRY` | launch-readiness | live Stripe price ID for the one-time $49 Pantry Review |
| `MEAL_EXTRACT_STUB` | dev/test only | `1` returns a fixed meal-photo draft without a vision call (`lib/meal/photo-extract.ts`). Ignored in production builds |
| `ADMIN_EMAIL` | runtime | The one email address `lib/server/admin.ts` treats as the founder/admin identity for the admin surfaces. Unset ⇒ no session is admin (fail closed) |
| `PAL_ENFORCE_COMPONENT_MENTION` | runtime engine toggle | `lib/pal/postprocess.ts`: opts into the stricter component-mention check on model output. Unset keeps the default posture |
| `PHOTO_INPUT_ENABLED` | owner/evidence gate | **Server twin / runtime kill switch** for `NEXT_PUBLIC_PHOTO_INPUT` (`lib/photo-input-flag.ts` `photoInputServerEnabled`): only exact `1` serves `/api/check/photo-draft`; an env change + redeploy kills the paid vision boundary without a rebuild. Production builds with the client flag `1` fail unless this is also `1` (twin guard) |
| `LONGITUDINAL_INSIGHTS_ENABLED` | owner/evidence gate | **Server twin / runtime kill switch** for `NEXT_PUBLIC_LONGITUDINAL_INSIGHTS` (`lib/longitudinal-insights-flag.ts`): only exact `1` permits server-side insight output — `app/api/coach/route.ts:62` nulls `view.insight` otherwise, so an env change + redeploy kills derived pattern output without a rebuild. Same twin-guard rule as the photo pair. **Set to `1` in production — observed at runtime 2026-08-15T15:36Z**, `GET /api/health` → `flagTwins.longitudinalInsights: "on"`. ⚠️ `GET /api/coach` still 401s before the flag branch and is NOT a probe: signed in, it returns `insight: null` both when the flag is off and when `deriveInsight` bails under `MIN_CHECKS_FOR_INSIGHT`. `/api/health` reports all four twins as boolean-only state; read it there. Read per request, so re-read after any env change |
| `NEXT_PUBLIC_PHOTO_INPUT` | owner/evidence gate | **Meal photo-assist gate** (`lib/photo-input-flag.ts`): only exact `1` renders the control and permits `/api/check/photo-draft`; unset, `0`, and every other value return `404` before model use. **Set to `1` in production** — owner-authorized 2026-08-14 (`docs/legal/owner-decision-2026-08-14-photo-assist-on.md`); keep unset in preview unless a preview is deliberately exercising the enabled path. ⛔ Turning it off again requires re-capturing `public/landing/app-check.png` in the same change — that screenshot pictures the photo chip and no test can read pixels. Tests that exercise enabled behavior must opt in explicitly. Build-time variable — changing it requires a newly reviewed build and redeploy |
| `NEXT_PUBLIC_LONGITUDINAL_INSIGHTS` | owner/evidence gate | **Longitudinal-insights gate** (`lib/longitudinal-insights-flag.ts`): only exact `1` permits derived pattern output or its product/paid promises. Unset, `0`, and every other value produce no coach insight in server payloads, guest/signed-in dashboards, or the daily loop. **Set to `1` in production** — owner-authorized 2026-08-14 (`docs/legal/owner-decision-2026-08-14-longitudinal-insights-on.md`); keep unset in preview unless a preview is deliberately exercising the enabled path. Verifiable live and unauthenticated: `/privacy` renders `derived pattern summaries` and `personalized pattern summaries` only when this is `1` (`app/(app)/privacy/page.tsx:72,74`). The insight is **free**, not Premium — the entitlement gate in the coach route covers progress/BAI only. Build-time variable — changing it requires a newly reviewed build and redeploy |
| `NEXT_PUBLIC_MEAL_MEMORY` | WS-5 rollout gate | **Meal-memory client build flag** (`lib/meal-memory-flag.ts` `mealMemoryUiEnabled`): only exact `1` renders the save affordance and the Saved-meals surfaces; inlined at build time (change ⇒ reviewed rebuild). Production builds with this `1` **fail unless `MEAL_MEMORY_ENABLED=1` is also set** (twin guard in `next.config.ts`, AUD-002). Flip only per the WS-5 rollout order: server twin first, then client. |
| `MEAL_MEMORY_ENABLED` | WS-5 rollout gate | **Meal-memory server twin / runtime kill switch** (`mealMemoryServerEnabled`; read by `lib/server/capabilities.ts`): only exact `1` serves the memory API routes — anything else 404s them, so an env change + redeploy disables the feature without a rebuild. Rollback = unset this (the client surfaces then hit inert endpoints designed to fail quiet). |
| `NEXT_PUBLIC_LEARNING_JOURNEY` | WS-5 rollout gate | **Learning-journey client build flag** (`lib/learning-journey-flag.ts` `learningJourneyUiEnabled`): only exact `1` renders journey surfaces; build-time. Production builds with this `1` **fail unless `LEARNING_JOURNEY_ENABLED=1` is also set** (twin guard, AUD-002). Do not flip until the AUD-007 recap copy is live (WS-6) — the landing must not advertise an artifact the journey doesn't render. Server twin first, then client; Memory before Journey. |
| `LEARNING_JOURNEY_ENABLED` | WS-5 rollout gate | **Learning-journey server twin / runtime kill switch** (`learningJourneyServerEnabled`; read by `lib/server/capabilities.ts`): only exact `1` serves `/api/journey` — anything else 404s it. Journey nudges are authoritative only while this is on; the pause/graduate stop-state persists across a flag-off rollback (AUD-019). Rollback = unset this. |
| `PANTRY_EXTRACT_STUB` | dev/test only | same idea for the Pantry Review photo extraction |
| `PANTRY_BLOB_READ_WRITE_TOKEN` | Pantry private storage | Credential for a dedicated **private** Vercel Blob store. Required by upload, authenticated vision reads, deletion, and orphan listing. Do not reuse the legacy public-store `BLOB_READ_WRITE_TOKEN`; Vercel store access mode is immutable, so provision and bind a new private store before enabling Pantry. |
| `PAL_DAILY_CHECK_CAP` | existing | **GLOBAL** daily check cap — all users combined, enforced by the middleware (default 2000, `lib/pal/rate-limit.ts`). It is **not** per-user; the per-user model-spend limit is the separate `check_user` bucket (200/24h per userId, RE-04), enforced in the check handler and not configurable by this variable |
| `NEXT_PUBLIC_WAITLIST_URL` | launch-readiness | Tally waitlist form URL for `/get-the-app`; section hidden when unset |
| ⚙ `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` | P5 | `npx web-push generate-vapid-keys`; also expose the public key as `NEXT_PUBLIC_VAPID_PUBLIC_KEY` |
| ⚙ `CRON_SECRET` | P5 | bearer token the schedulers send to cron routes — the Railway `hourly-crons` service (nudge, pantry-sweep, trial-precharge, stripe-reconcile; `docs/runbooks/price-test.md`) and the one remaining Vercel cron (`bai-weekly`, `vercel.json`). `stripe-reconcile` (`/api/cron/stripe-reconcile`, Task 8 / P2.2) hourly retries the durable Stripe event inbox and verifies subscriptions against Stripe — it is the SLO backstop for missed webhooks. |
| `NEXT_PUBLIC_UMAMI_SRC` | P7 | Umami tracker script URL (`docs/adr/analytics-umami.md`); analytics is fully disabled (no `<script>` rendered, `track()` no-ops) when unset. Production currently points at **umami cloud** (`https://cloud.umami.is/script.js`), not the self-hosted Railway install the ADR describes. Its origin (https only) feeds CSP `script-src` **and** `connect-src` |
| `NEXT_PUBLIC_UMAMI_HOST_URL` | 2026-07-22 | Optional. Origin the tracker POSTs events **to**, when that differs from the script host. Umami cloud serves the script from `cloud.umami.is` but its build hardcodes `https://gateway.umami.is/api/send` as the ingest default, so `next.config.ts` adds that gateway to `connect-src` automatically whenever the script origin is umami cloud — set this only to point ingest somewhere else. Self-hosted installs serve script and ingest from one origin and need neither. Getting this wrong is silent: every `track()` call is CSP-refused and the funnel records nothing while the page looks fine (found live 2026-07-22) |
| `NEXT_PUBLIC_UMAMI_WEBSITE_ID` | P7 | Umami website ID; both this and `NEXT_PUBLIC_UMAMI_SRC` must be set for analytics to activate |
| `NEXT_PUBLIC_SENTRY_DSN` | P0.3 | Client-side error reporting (`components/client-error-reporting.tsx`); messages/breadcrumbs/query strings are stripped before send. Without it, browser errors are invisible. Its origin (https only) is also derived into the CSP `connect-src` in `next.config.ts` — without that the browser transport ships but every envelope POST is CSP-blocked and errors never arrive (FINDING-031) |
| `PAL_ALLOW_NO_MEASUREMENT` | P0.3 | Exact `1` waives the production build gate that otherwise fails when the Umami/Sentry client vars are unset (`next.config.ts`). Use only for a deliberate dark launch |
| `NEXT_PUBLIC_APP_URL` | 4D/P7 | canonical origin — `https://prediabetespal.com` in production (was `https://revora.plus` until 2026-08-11; that host now 301s here). Never `revora.app`: that domain belongs to an unrelated company |
| ⚙ `REVIEWER_TEST_SECRET` | P9 | shared secret for the Play-reviewer test-login bypass (`app/api/auth/reviewer-signin/route.ts`). Set in **preview only** — the route hard-404s in production regardless of this value (`VERCEL_ENV`/`NODE_ENV` check), so leaving it unset in production is a second, redundant lock, not the only one |
| `NEXT_PUBLIC_REVIEWER_MODE` | P9 | set to `1` in **preview only** to show the "Reviewer access" form on `/signin`. **Never set in production** — this is a build-time constant, so leaving it unset makes the form permanently inert (`false`) in the production bundle |
| ~~`SUPPORT_EMAIL`~~ | **retired 2026-07-21** | Now the constant `SUPPORT_EMAIL` in `lib/pal/contact.ts`. It was never settable on `/progress` (a `"use client"` page cannot read a non-`NEXT_PUBLIC_` var), so that surface hardcoded a literal and drifted; the address is public and identical in every environment, so the variable bought nothing and hid a hole. `tests/unit/pal/owned-domains.test.ts` now fails the build if any address lands on a domain we do not own |
| ⚙ `AUTH_EMAIL_FROM` | P5 | envelope sender override for preview. Defaults to `Prediabetes Pal <signin@contact.prediabetespal.com>` (`lib/pal/contact.ts:53`) — `contact.prediabetespal.com` is the domain verified in Resend, live since 2026-08-10 (was `contact.revora.plus`, verified 2026-07-21; last send 2026-07-23). ⚠️ `contact.ts:41` calls this "preview-only", but the var **is** set in Vercel Production and therefore wins over that fallback — both currently spell the same address, so verify from the provider side (`GET api.resend.com/emails`) rather than trusting either, since the value is sensitive and unreadable. The sending domain must be verified in Resend with DKIM published, or every magic link silently fails SPF/DKIM alignment and lands in spam — which on a passwordless product is a login outage, not a deliverability nit |
| ⚙ `SUPPORT_INBOX_EMAIL` | P0.4 | where the app's INTERNAL notifications (support-case copies, pantry-sweep alerts) are delivered (`supportInbox()` in `lib/server/email.ts`). Falls back to the public `SUPPORT_EMAIL` constant when unset. Needed because `revora.plus`'s MX is Namecheap email forwarding, whose relays greylist Resend's sending IPs — observed 2026-07-22: 3/3 Resend→`support@revora.plus` sends bounced while 4/4 Resend→Gmail delivered. Unlike the retired `SUPPORT_EMAIL` var above, this one is not public-facing and genuinely varies by environment, so an env var is correct here |
| `LEGAL_ENTITY_NAME` | P9/legal | public operator name rendered in Terms and Privacy. It must be factual and never a placeholder. The owner WTP decision for `5f6abcb` authorizes the brand name `Revora`; it does not establish a separate legal entity or address |
| `NEXT_PUBLIC_PLAY_BILLING` | owner/evidence gate | **Google Play billing gate** (`lib/play-billing-flag.ts`): only exact `1` permits Digital Goods purchase UI and new receipt verification. Keep unset for the Stripe-only WTP launch; changing it requires a separate reviewed build and explicit owner decision |
| `PAL_LAUNCH_MODE_OVERRIDE` | dev/test only | set to `paused` to simulate a launch-controls incident (`lib/pal/launch-controls.ts`) in unit/smoke tests without touching live Edge Config. **Never set in production** — it is ignored whenever `NODE_ENV` or `VERCEL_ENV` is `production`, so this is a dev/test-only override, not a production kill switch (use `EDGE_CONFIG`'s `launch_mode` key for that) |

Local dev bootstrap:

```bash
# .env.local (never committed)
openssl rand -base64 32   # → AUTH_SECRET
openssl rand -base64 32   # → HEALTH_DATA_KEY
npx web-push generate-vapid-keys   # → VAPID_*
```

Migrations: use `DATABASE_MIGRATION_URL` for schema ownership and keep
`DATABASE_URL` on the restricted app role. For production run
`PAL_DB_ENV=production DATABASE_URL=<runtime-url> DATABASE_MIGRATION_URL=<owner-url> npm run db:migrate:production`, then
`npm run db:governance:check` (run per environment: dev → preview → prod).
The canonical production database is **Neon** project
`dry-shadow-56131409`, database `neondb` (since the 2026-08-10 outage
migration — the Railway service `Postgres` and its `Postgres-D2oG` /
`Postgres-FOMu` orphans this paragraph used to name are retired). Direct
connections from non-allowlisted IPs are reset; use the Neon console or an
allowlisted host. The
production journal was baselined 2026-07-19 (`drizzle.__drizzle_migrations`
seeded 0000–0012); if a database was ever schema-pushed by hand instead of
migrated, re-baseline with
`DATABASE_MIGRATION_URL=<url> node scripts/baseline-drizzle-journal.mjs` (dry-run flag
available) before trusting `drizzle-kit migrate`.
